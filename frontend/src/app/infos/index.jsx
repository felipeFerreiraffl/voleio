import { Text, View } from 'react-native';

export default function infos() {
 return (
   <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <Text style={{ fontWeight: "bold" }}>Essa é a página das informações do cabra</Text>
   </View>
  );
}